version: '3.8'

services:
  app:
    build: .
    container_name: hrm_app
    restart: always
    ports:
      - "8082:80"
    volumes:
      - .:/var/www/html
    environment:
      - TERM=xterm
      - DB_HOST=172.17.0.1
      - DB_USER=root
      - DB_PASS=
      - DB_NAME=hrm_db
